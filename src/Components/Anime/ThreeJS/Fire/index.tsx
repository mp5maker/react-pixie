import * as React from 'react'
import * as THREE from 'three'
import { useFrame, useResource, useThree } from 'react-three-fiber'

export const Fire = ({ color }: any) => {
    return (
        <>
        </>
    )
}